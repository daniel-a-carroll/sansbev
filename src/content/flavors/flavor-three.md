---
name: '[[PLACEHOLDER — SKU 3 flavor name]]'
order: 3
status: concept
tagline: '[[PLACEHOLDER — short flavor tagline]]'
description: '[[PLACEHOLDER — one or two sentences. Composition and taste, not effect.]]'
color:
  field: '#2C4B7C'
  onText: '#F1F3EF'
  ink: '#2C4B7C'
---

[[PLACEHOLDER — longer flavor description. This markdown body renders as prose on
the flavor page. Everything above the line is structured data and feeds both the
page and the Product JSON-LD.

Fields not yet filled in — add them to the frontmatter above as they are decided.
Each one is optional and its row or section is simply absent until it exists:

  tastingNotes:        list of strings
  ingredients:         full declared ingredient list, in label order
  allergenStatement:   string
  juicePercent:        number
  caffeineMg:          number
  lTheanineMg:         number
  canSizeFlOz:         number
  upc:                 12-14 digits
  nutrition:           see the schema in src/content.config.ts

Anything describing an effect on the body belongs in src/data/claims.ts, not here.]]
